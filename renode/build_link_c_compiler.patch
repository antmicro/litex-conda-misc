--- /home/adam/renode-hq/renode/tools/packaging/conda/build.sh
+++ /home/adam/renode-hq/renode/tools/packaging/conda/build.sh
@@ -3,6 +3,11 @@
 set -x
 
 if [[ "$(uname)" == 'Linux' ]]; then
+    ln -s $GCC $BUILD_PREFIX/bin/gcc
+fi
+ln -s $AR $BUILD_PREFIX/bin/ar
+
+if [[ "$(uname)" == 'Linux' ]]; then
     install -D /usr/bin/realpath $BUILD_PREFIX/bin/realpath
     install -D /bin/sed $BUILD_PREFIX/bin/sed
 
