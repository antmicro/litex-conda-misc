1c1
< @echo off
---
> @echo on
5a6,8
> 
> for /F "usebackq tokens=*" %%i in (`vswhere -latest -requires Microsoft.Component.MSBuild -find MSBuild\**\Bin\MSBuild.exe -property installationPath`) do (set MSBUILDDIR=%%i )
> set PATH=%MSBUILDDIR%;%PATH%
